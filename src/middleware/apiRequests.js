// import axios from 'axios';

// const apiRequests = apiUrl => store => next => action => {
//   if (!action.request) {
//     return next(action);
//   }

//   let REQUEST
// };

// export default apiRequests;
