import * as types from '../constants/actionTypes';

export const fetchUser = () => ({ type: types.FETCH_USER });
export const fetchProjects = () => ({ type: types.FETCH_PROJECTS });
export const fetchProject = () => ({ type: types.FETCH_PROJECT });
